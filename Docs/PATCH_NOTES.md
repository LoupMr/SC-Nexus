# SC-Nexus — Patch Notes

> **Discord (max 2000 chars)** — Copy the block below to post.

---

```
📢 **SC-NEXUS UPDATE** — Quality & Security Patch

🔒 **Security:** Login now shows generic "Invalid username or password" (no account enumeration). All APIs validate input; malformed JSON returns 400 instead of crashing.

⚡ **Performance:** Ledger loads faster (batch queries). Delete no longer refetches the whole list.

🐛 **Fixes:** Error screen with retry instead of blank page. Ledger shows fetch errors. Delete only refreshes on success. CSV export escapes commas/quotes. Quantity inputs handle empty/invalid values.

♿ **A11y:** Icon buttons have screen reader labels. Error banner has dismiss button.

Report issues if you see any. Thanks! 🚀
```
